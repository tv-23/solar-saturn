<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SEATRUST - Maritime Logistics</title>

    <!-- Bootstrap Icons & Google Fonts -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --sea-blue: #1a428a;
            --sea-orange: #F08B33;
            --dark: #0f172a;
            --white: #ffffff;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #f8fafc;
            overflow-x: hidden;
        }

        /* --- NAVIGATION WRAPPER --- */
        .ex2-nav {
            background: var(--sea-blue);
            color: white;
            position: relative;
            padding-bottom: 30px;
            width: 100%;
            overflow: hidden;
            z-index: 1000;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 100%;
            padding: 0 25px;
            box-sizing: border-box;
        }

        /* --- TOP BAR --- */
        .ex2-top {
            background: rgba(0, 0, 0, 0.15);
            height: 38px;
            font-size: 11px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .ex2-top a {
            color: white;
            text-decoration: none;
        }

        .top-socials-mobile {
            display: none;
            gap: 15px;
            font-size: 14px;
        }

        .top-right-contacts {
            display: flex;
            gap: 20px;
            align-items: center;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .phone-highlight {
            color: var(--sea-orange) !important;
            font-weight: 800;
        }

        /* --- MAIN HEADER (LEFT - CENTER - RIGHT) --- */
        .ex2-header {
            height: 95px;
            display: flex;
            align-items: center;
        }

        .header-left {
            flex: 1;
            display: flex;
            justify-content: flex-start;
        }

        .logo-box {
            font-weight: 800;
            font-size: 28px;
            letter-spacing: -1.5px;
            color: white;
            text-decoration: none;
        }

        .logo-box span {
            color: var(--sea-orange);
        }

        .header-center {
            flex: 2;
            display: flex;
            justify-content: center;
        }

        .ex2-links {
            display: flex;
            gap: 45px;
        }

        .ex2-links a {
            font-weight: 700;
            color: white;
            text-decoration: none;
            position: relative;
            font-size: 16px;
            letter-spacing: 0.2px;
            white-space: nowrap;
        }

        /* ===== ROUTE PROGRESS HOVER EFFECT ===== */
        .ex2-links a::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -15px;
            width: 100%;
            height: 2px;

            background: repeating-linear-gradient(
                90deg,
                var(--sea-orange) 0 10px,
                transparent 10px 18px
            );

            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.45s ease;
        }

        .ex2-links a:hover::after {
            transform: scaleX(1);
        }

        .header-right {
            flex: 1;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 25px;
        }

        .btn-call {
            background: var(--sea-orange);
            padding: 12px 28px;
            border-radius: 4px;
            font-weight: 700;
            color: white;
            text-decoration: none;
            font-size: 14px;
            transition: 0.3s;
            text-transform: uppercase;
            white-space: nowrap;
        }

        .btn-call:hover {
            background: #e07a22;
            transform: translateY(-2px);
        }

        .desktop-header-socials {
            display: flex;
            gap: 18px;
        }

        .desktop-header-socials a {
            color: white;
            font-size: 20px;
            transition: 0.3s;
        }

        .desktop-header-socials a:hover {
            color: var(--sea-orange);
        }

        .mobile-menu-toggle {
            display: none;
            font-size: 32px;
            cursor: pointer;
            color: white;
        }

        /* --- THE WAVE --- */
        .ex2-wave {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 200%;
            height: 45px;
            fill: #f8fafc;
            animation: waveOrbit 15s linear infinite;
            pointer-events: none;
        }

        @keyframes waveOrbit {
            0% {
                transform: translateX(0);
            }

            100% {
                transform: translateX(-50%);
            }
        }

        /* --- MOBILE MENU OVERLAY --- */
        .mobile-overlay {
            position: fixed;
            top: 0;
            right: -101%;
            width: 80%;
            height: 100vh;
            background: var(--sea-blue);
            z-index: 2000;
            display: flex;
            flex-direction: column;
            padding: 100px 40px;
            transition: 0.5s cubic-bezier(0.8, 0, 0.2, 1);
            box-shadow: -10px 0 30px rgba(0, 0, 0, 0.5);
        }

        .mobile-overlay.active {
            right: 0;
        }

        .mobile-overlay a {
            color: white;
            text-decoration: none;
            font-size: 24px;
            font-weight: 800;
            margin-bottom: 25px;
        }

        .mobile-close {
            position: absolute;
            top: 25px;
            right: 25px;
            font-size: 32px;
            color: white;
            cursor: pointer;
            transition: 0.3s;
        }

        .mobile-close:hover {
            color: var(--sea-orange);
        }

        /* ==========================================
           RESPONSIVE LOGIC
        ========================================== */
        @media (max-width: 992px) {
            .top-left-text,
            .header-center,
            .desktop-header-socials,
            .contact-text {
                display: none;
            }

            .top-socials-mobile {
                display: flex;
            }

            .mobile-menu-toggle {
                display: block;
            }

            .header-left,
            .header-right {
                flex: 1;
            }

            .ex2-header {
                height: 80px;
            }

            .logo-box {
                font-size: 22px;
            }

            .btn-call {
                padding: 8px 15px;
                font-size: 12px;
            }
        }
    </style>
</head>

<body>

    <div class="ex2-nav">
        <!-- TOP BAR -->
        <div class="ex2-top">
            <div class="container">
                <div class="top-left">
                    <span class="top-left-text">SEATRUST LOGISTICS — MARITIME SOLUTIONS</span>
                    <div class="top-socials-mobile">
                        <a href="#"><i class="bi bi-facebook"></i></a>
                        <a href="#"><i class="bi bi-linkedin"></i></a>
                        <a href="#"><i class="bi bi-twitter-x"></i></a>
                    </div>
                </div>

                <div class="top-right-contacts">
                    <a href="https://www.google.com/maps/place/Seatrust+Logistics/@33.5972387,-7.6179703,17z/data=!3m1!4b1!4m6!3m5!1s0xda7d2831388b2cb:0xabbebd7ebdc68e5a!8m2!3d33.5972343!4d-7.6153954!16s%2Fg%2F11b6jgxw88?entry=ttu&g_ep=EgoyMDI2MDExMy4wIKXMDSoKLDEwMDc5MjA3M0gBUAM%3D" class="contact-item" target="_blank">
                        <i class="bi bi-geo-alt"></i>
                        <span class="contact-text">12, Rue Oraibi Jilali, Casablanca</span>
                    </a>

                    <a href="mailto:contact@seatrust.ma" class="contact-item" target="_blank">
                        <i class="bi bi-envelope-fill"></i>
                        <span class="contact-text">contact@seatrust.ma</span>
                    </a>

                    <a href="tel:+212522435090" class="contact-item phone-highlight" target="_blank">
                        <i class="bi bi-telephone-fill"></i>
                        <span class="contact-text">+212 522 435 090</span>
                    </a>
                </div>
            </div>
        </div>

        <!-- MAIN HEADER -->
        <header class="ex2-header">
            <div class="container" style="width: 100%;">
                <div class="header-left">
                    <a href="/" class="logo-box">SEA<span>TRUST</span></a>
                </div>

                <div class="header-center">
                    <nav class="ex2-links">
                        <a href="#">Home</a>
                        <a href="#">Seatrust</a>
                        <a href="#">Our Companies</a>
                        <a href="#">Partners</a>
                        <a href="#">Contact Us</a>
                    </nav>
                </div>

                <div class="header-right">
                    <a href="tel:+212522435090" class="btn-call">Appelez-nous</a>
                    <div class="desktop-header-socials">
                        <a href="#"><i class="bi bi-linkedin"></i></a>
                        <a href="#"><i class="bi bi-twitter-x"></i></a>
                    </div>
                    <!-- Hamburger Icon -->
                    <div class="mobile-menu-toggle" onclick="toggleMenu()">
                        <i class="bi bi-list" id="menuIcon"></i>
                    </div>
                </div>
            </div>

            <svg class="ex2-wave" preserveAspectRatio="none" viewBox="0 0 1440 100">
                <path d="M0,40 C320,100 420,0 720,40 C1020,80 1120,0 1440,40 L1440,100 L0,100 Z"></path>
                <path d="M0,50 C320,110 420,10 720,50 C1020,90 1120,10 1440,50 L1440,100 L0,100 Z" opacity="0.3"></path>
            </svg>
        </header>
    </div>

    <!-- MOBILE OVERLAY MENU -->
    <div class="mobile-overlay" id="mobileOverlay">
        <!-- Close Button Inside Overlay -->
        <div class="mobile-close" onclick="toggleMenu()">
            <i class="bi bi-x-lg"></i>
        </div>

        <a href="#">Solutions</a>
        <a href="#">Fleet</a>
        <a href="#">Network</a>
        <hr style="border:0; border-top:1px solid rgba(255,255,255,0.1); margin: 20px 0;">
        <div style="display:flex; gap:25px; font-size: 30px;">
            <a href="#"><i class="bi bi-linkedin"></i></a>
            <a href="#"><i class="bi bi-facebook"></i></a>
            <a href="#"><i class="bi bi-twitter-x"></i></a>
        </div>
    </div>

    <script>
        window.toggleMenu = function () {
            const overlay = document.getElementById('mobileOverlay');
            const icon = document.getElementById('menuIcon');
            overlay.classList.toggle('active');

            // Synchronize the header hamburger icon state
            if (overlay.classList.contains('active')) {
                icon.classList.replace('bi-list', 'bi-x-lg');
            } else {
                icon.classList.replace('bi-x-lg', 'bi-list');
            }
        }
    </script>

</body>

</html>
